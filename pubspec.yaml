name: ungdungnhatky
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.3

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:      # ✅ Thêm đúng chỗ
    sdk: flutter
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.3
  path: ^1.9.0
  provider: ^6.1.2
  flutter_local_notifications: ^18.0.0
  timezone: ^0.9.4
  camera: ^0.10.5+9
  permission_handler: ^11.3.0
  saver_gallery: ^3.0.6
  video_player: ^2.8.3
  image_picker: ^1.0.7
  uuid: ^4.3.3
  flutter_screen_recording: ^2.0.0
  video_thumbnail: ^0.5.3
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^2.0.1
  build_runner: ^2.4.6
  flutter_lints: ^5.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true
  assets:
    - assets/images/nhatky1.jpg

flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/nhatky1.jpg"